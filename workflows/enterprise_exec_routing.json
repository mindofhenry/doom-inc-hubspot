{
  "_comment": "DOOM Inc — Enterprise Exec Routing workflow spec. Requires HubSpot Sales Hub Professional or higher to deploy via API or UI. Direct owner assignment requires Operations Hub. This workflow uses a property flag (last_touched_by = AE) as a free-tier-compatible alternative.",
  "name": "DOOM Inc — Enterprise Exec Routing (AE Tag)",
  "type": "CONTACT_BASED",
  "trigger": {
    "type": "AND",
    "conditions": [
      {
        "type": "CONTACT_PROPERTY",
        "property": "gtm_segment",
        "operator": "EQ",
        "value": "Enterprise"
      },
      {
        "type": "CONTACT_PROPERTY",
        "property": "gtm_persona",
        "operator": "IN",
        "values": ["CISO", "VP of Security"]
      }
    ]
  },
  "enrollment": {
    "re_enrollment": false,
    "trigger_on_create": true
  },
  "actions": [
    {
      "type": "SET_CONTACT_PROPERTY",
      "property": "last_touched_by",
      "value": "AE",
      "_note": "Proxy for owner assignment. Direct owner rotation requires Operations Hub Professional."
    }
  ]
}
